<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= farm.name %> </title>
</head>
<body>
    <h1> <%= farm.name %> </h1>
    <ul>
        <li>Name: <%= farm.name %> </li>
        <li>City: <%= farm.city %> </li>
        <li>Email: <%= farm.email %> </li>
    </ul>
    <br>
    <h2>Products</h2>
    <ul>
        <% for(let product of farm.products) {%>
            <li><a href="/products/<%=product._id%>"><%= product.name %> </a></li>
            <% } %> 
    </ul>
    <br>

    <form action="/farms/<%=farm._id%>?_method=DELETE" method="POST">
        <button class="submit">Delete Both</button>
    </form>

    <br>
    <a href="/farms">Back to farms</a>
    <br>
    <a href="/farms/<%=farm._id%>/edit">Edit farm</a>
    <a href="/farms/<%=farm._id%>/products/new">New product for this farm</a>
    <form action="/farms/<%=farm._id%>?_method=DELETE" method="POST">
        <button>Delete</button>
    </form>
</body>
</html>